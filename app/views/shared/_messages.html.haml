- @messages.each do |message|
  .message__conteiner
    .message__conteiner__content
      .transaction-field
        .user-message
          .user-message__face
            .wrrap
              = fa_icon("circle 3x")
              -# %span "#{@group.messages.user}"
          .user-message__body
            .user-message__body__text
              = message.content
              %br
              = message.created_at.strftime("%Y年%m月%d日 %H:%M")